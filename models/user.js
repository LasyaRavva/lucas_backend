// User model for documentation
// Supabase handles user storage, but you can extend with profile data
// Table: profiles
// Columns: id (uuid, PK, FK to auth.users), name (text), streak (int), xp (int), level (text)
